// lib/src/utils/logger.dart
void logError(String code, String? message) {
  // Replace with your preferred logging mechanism (e.g., print, logger package)
  print('[$code] ${message ?? 'No message provided'}');
}
